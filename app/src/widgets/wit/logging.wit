// Allows plugins to write logs to the host
default interface logging {
    enum level {
        debug,
        info,
        warn,
        error
    }

    log: func(level: level, context: string, message: string)
}